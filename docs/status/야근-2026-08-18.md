# 야근 보고 — 2026-08-18

**브랜치** `야근/2026-08-18-dog-id-unique` (main 에서 분기, **푸시 안 함**)
**한 줄**: KE-T2 **FAIL** — 축 통일(흡수) 기각. 실패 이유가 데이터가 아니라 **교육 철학 비호환**이라 대안이 3개로 갈렸다. 선결 과제(`dog_id` UNIQUE)는 **게이트가 없어 착수하지 않았다.**

## 1. 돌려본 것

```
bash scripts/check-harness.sh              → Harness check passed.
npx tsc --noEmit                           → TypeScript: No errors found
npx eslint .                               → 0 errors
npm run test:app                           → 202 passed, 202 total
npm run test:edge                          → 163 passed, 163 total
node scripts/ke/ke-t2-progress-absorption.mjs --gate → exit 2 (FAIL 판정)
```
착수 전 baseline 도 위와 동일하게 전부 green 이었다 — **내가 깬 것은 없다.**

Backend:
```
cd Backend && rtk proxy python3 -m pytest tests -q
→ ImportError: No module named 'fastapi'   (venv 없음, 의존성 미설치)
```

## 2. 되는 것

**KE-T2 측정 경로가 끝에서 끝까지 돈다.**
`curriculum.ts` 108스텝 추출 → tailtree 사다리 생성 → bigram 매핑 → 2지표 산출 → JSON 아티팩트 → 게이트 exit code.

| 커리큘럼 | 구 | 신 | 배수 | 최대편차 |
|---|---|---|---|---|
| basic_obedience | 15 | 104 | 6.9× | 0.761 |
| fear_desensitization | 15 | 79 | 5.3× | 0.702 |
| impulse_control | 15 | 109 | 7.3× | 0.991 |
| leash_manners | 18 | 99 | 5.5× | 0.864 |
| reactivity_management | 15 | 142 | 9.5× | 0.944 |
| separation_anxiety | 15 | 104 | 6.9× | 0.799 |
| socialization | 15 | 79 | 5.3× | 0.787 |

**판정**: 매핑 실패 99/108 (임계 0) · 최대편차 **99.1%p** (임계 10%p) → **FAIL**

## 3. 안 되는 것 (미검증 명시)

- **Backend 는 아무것도 검증 못 했다.** `fastapi`·`sqlalchemy` 미설치, venv 없음. `requirements.txt` 전량 설치는 야근 울타리(100MB)에 걸려 무인 실행에서 하지 않았다.
- 실제 사용자 진행 분포를 모른다 — 균등 가정으로 계산했다. 대부분이 Day1~2 에 몰려 있으면 손실이 작을 수 있다.
- **(a) 매핑 실패 99건은 신뢰하지 말 것.** 눈으로 대조하니 주제적으로는 정확했다("외출복 갖춰 입기" → `sep_cue_desens`, "문 열고 3초" → 같은 원자 step4). 한글 문자 bigram 이 다른 표현을 못 잡는 **지표 한계**다. 구속력 있는 실패는 **(b) 편차** 하나다.

## 4. 막힌 것

**선결 1번(`user_training_status` UNIQUE 에 `dog_id` 추가 + SQLAlchemy `UniqueConstraint` 선언) 은 착수하지 않았다.**

- 시도 1: `pytest tests -q` → RTK 훅이 실행을 가로채 "No tests collected" 만 반환
- 시도 2: `rtk proxy python3 -m pytest tests -q` → 진짜 원인 확인 = `ModuleNotFoundError: No module named 'fastapi'`
- 판정: **Backend 게이트가 오늘 밤 존재하지 않는다.** 야근 규율 — *"게이트가 없는 기존 레포는 야근 대상이 아니다"*, *"밤에 새 게이트를 세우면 그 게이트 자체가 미검증"*.
  DB 마이그레이션을 검증 없이 밤에 쓰는 것은 정확히 이 규칙이 막는 종류다.

**해제 방법 (사람 있을 때 10분)**: `cd Backend && python3 -m venv .venv && .venv/bin/pip install -r requirements.txt` → `pytest` baseline 확보. 그 뒤엔 마이그레이션 1개 + 모델 1줄이라 짧다.

## 5. 다음 한 수

**KE-T2 FAIL 이 낳은 대안 3안 중 택1을 사람과 함께 수렴한다.**

실패 원인이 데이터가 아니라 **설계 철학**이다:

| | 구 커리큘럼 | 신 사다리 |
|---|---|---|
| 진입 | **문제 행동부터 바로** (Day1 = 외출 신호 둔감화) | **기초부터** (마커·보상·주의·매트… 약 90스텝 뒤 도착) |
| `sep_cue_desens` 위치 | **0.07** | **0.86** |

그래서 진행률을 옮기면 7% → 86% 로 튀고, 반대로 두면 "3일차까지 했는데 처음부터"가 된다. **어느 쪽도 진실이 아니다.**

- ① 두 축 공존 (2차 수렴 R2 기각안 — 분기 비용 감수)
- ② 기존 사용자는 구 커리큘럼 유지, 신규만 그래프
- ③ **신 사다리에 진입 평가를 붙여 아는 기초를 건너뛰게 한다** — 노드마다 `assessmentPrompt` 필드가 **이미 있다**. KE-T2 가 드러낸 신규 후보

> **재수렴은 하지 않았다.** 야근 함정 — *"밤에는 반박할 사람이 없어 판정이 후해진다."* 3안 선택은 사람이 있을 때.

## 6. 버리려면

```
git switch main && git branch -D 야근/2026-08-18-dog-id-unique
```
앱 코드(`src/`·`Backend/`·`supabase/`) **변경 0건** — 지워도 잃는 건 이 측정 결과뿐이다.

## 참조

- `docs/goals/GOAL-ke-t2-progress-absorption.md` §7 — 실행 기록·지표 한계·눈 대조
- `docs/ref/rnd/KE-T2-RESULT.json` — 108건 매핑 원본
- `docs/ref/rnd/TRAINING-GRAPH-PORT-CONVERGE-LOOP-2026-08-18.md` §10-7 — P7 #4 무효 기록
