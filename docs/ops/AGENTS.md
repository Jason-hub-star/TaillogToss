# docs/ops agent rules

- Scope: `docs/ops/*` operational playbooks for agent usage only.
- Keep files pointer-oriented and tied to real commands or documents in this repo.
- Do not duplicate long project status here; link back to `docs/status/*`.
- If a new ops document adds a recurring workflow, also reflect it in `HARNESS-MANIFEST.yaml` or `.claude/commands/*`.
