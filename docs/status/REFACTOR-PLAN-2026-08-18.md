# TaillogToss 리팩토링안 — 2026-08-18 롤업

하루치 `조준 → 정비 → 수렴 ×4 → 킬실험 ×5 → 감사 ×2` 결과를 한 장으로 접었다.
각 절의 근거는 링크된 SSOT 에 있다. **이 문서는 색인이지 정본이 아니다.**

## 0. 지금 어디

| | |
|---|---|
| `main` | `71510f1` = `origin/main` (동기) |
| `야근/2026-08-18-dog-id-unique` | `f5058b0` — **main 보다 7커밋 앞, 미푸시** |
| working tree | clean |
| 앱 코드 변경 | **0줄** (오늘 전부 문서·스크립트·하네스) |

야근 브랜치의 7커밋 = 킬실험 KE-T2/T4/T5 + 수렴 3·4차 + 감사 재라벨. **병합 여부는 주인 판정.**

## 1. 오늘 잠긴 결정 (LOCKED)

| # | 결정 | 근거 |
|---|---|---|
| L1 | **TaillogToss 동결 해제.** tailtree 와 **독립 병행**, `main` 에서 작업 | 주인 판정. tailtree `PLATFORM-ARCHITECTURE.md:11` 수정 완료 |
| L2 | **Plan A/B/C 폐기** | 주인 판정 ("나누는 것도 필요 없을 듯") |
| L3 | **과금 대폭 개편** — 무료 비중 상향, 유료 범위 재설정 | 주인 판정 |
| L4 | **ABC 기록 부담 경감** | 주인 판정 |
| L5 | **갈아엎기는 훈련 시스템만(A).** tailtree 이주(B)는 **반대** | 실측 **69,325줄 vs 267줄** — tailtree 엔 앱이 없다 |
| L6 | **지금이 마지막 공짜 창** — 실사용자 0(미출시)이라 마이그레이션 비용 0. 출시 후엔 **99.1%p** 청구서 | KE-T2 |

## 2. 킬-실험 5개 — 무엇이 죽고 무엇이 살았나

| 실험 | 물음 | 결과 | 남은 것 |
|---|---|---|---|
| **KE-T1** | how 카드가 증상→목표 경로를 연속으로 채우나 | **PASS** | 원자 **207/310 = 66.8%**(15.2%는 오측정이었다). 행동문제 도메인 **결손 0**, 10경로 전부 충전율 1.000 |
| **KE-T2** | 진행률을 잃지 않고 흡수 가능한가 | **FAIL → 무효** | 편차 **99.1%p**. 원인은 좌표가 아니라 **교육 철학**(구=문제부터, 신=기초부터). **실사용자 0 이라 적용 대상 소멸** |
| **KE-T3** | 108스텝을 노드에 붙였을 때 자연스러운가 | **미실행** | L2(A/B/C 폐기)로 **전제가 바뀌어 재검토 필요** |
| **KE-T4** | 규칙 파서가 LLM 없이 얼마나 맞추나 | **FAIL** | vet **1/11**. 단 **주제 라우팅 0.477 = 기준선 0.125 의 3.8배** — 이 축은 살았다 |
| **KE-T5** | 300토큰 LLM 스크리닝이 의학 신호를 다 잡나 | **FAIL** | vet **8/11**, **217토큰**(코칭의 1/37), 정밀도 0.489. **정교한 프롬프트가 오히려 손해**였다 |

**감사 재라벨**: `trainer` 라벨 **16건 중 6건(37.5%)이 과잉**, `none` 중 [22]는 **심장병·폐수종 명시 P0 오류**. 재라벨 시 의뢰 재현율 **0.815 → 0.917**.

## 3. 리팩토링안

### Layer 0 — 하네스 ✅ 완료

- `main` 정상화(60커밋 뒤처짐 + 316건 미봉합 해소)
- 메타 5종 복구(`/상태`·`/다음`·`/진단`·`/마감`·`/명령어`) + 진입점 4곳 배선
- `/감사` 이식(TaillogToss 급소 8 페르소나)
- **lint 게이트 실동작화** — 213 → 0 (155건은 설정 누락, 25건은 RN 부적합 규칙, 33건이 실제 코드)

### Layer 1 — 선결 (착수 가능, 순서 무관)

| # | 할 일 | 상태 |
|---|---|---|
| 1-1 | `user_training_status` UNIQUE 에 **`dog_id` 추가** + SQLAlchemy `UniqueConstraint` 선언 | **막힘** — Backend 의존성 미설치로 게이트 없음. `python3 -m venv .venv && pip install -r requirements.txt` 로 10분에 해제 |
| 1-2 | 감사 **A1** — 의학 인접 주제(마킹·배변·식욕·통증·이동)에 **상시 안내 문구** | 미착수. **KE-T5 가 이걸 안전 설계의 중심으로 승격** |
| 1-3 | 감사 **A3** — 코칭 `prompts.py:53` 이 만드는 "관련 훈련 바로 시작하기" 버튼 처리 | 미착수 |

### Layer 2 — 훈련 시스템 교체

| # | 할 일 | 근거 |
|---|---|---|
| 2-1 | 노출 부분그래프(**원자 207 · 엣지 326 · how 214 = 409KB**)를 **앱 번들에 적재**. 서버 아님 | 3차 P11 #2 — 오프라인 유지(감사 A4) |
| 2-2 | 증상 **17종** 병행 진입 추가. `BehaviorType` 10 유지 | 신규 진입점 7개(마킹·배변·핸들링·이동·기본신호·에너지·재주) |
| 2-3 | **빌드타임 필터** — `how[].authored === 'reviewed'` 통과분만 번들. 런타임 게이트 코드 없음 | 감사 A2(`provenance.review` 는 존재하지 않는 필드였다) |
| 2-4 | 108스텝 처리 **재검토** | L2 로 3차 P11 #5 의 전제(A/B/C 보존)가 사라졌다 |
| 2-5 | 신규 UI 3종 — **막힘 처방 시트**(`stuck`) · 선행 계보 패널 · 증상 칩 | 재사용 11 / 수정 10 / 신규 3 |
| 2-6 | `DayTabBar` 등 **Day 축 UI 4종 재설계** + 단계 접기 | 경로가 16~29 원자라 세로 타임라인이 깨진다 |
| 2-7 | `PlanVariant`·`planMeta`·`VariantSelector`·`recommendPlan` **제거** | L2 |

### Layer 3 — ABC 기록 경량화

현행 `ABCForm` = **자유 텍스트 5칸**. 개인화의 연료인데(`getRecommendationsV2(…, logAnalytics, …)`) 부담이 커서 안 쌓인다.

| # | 할 일 | 비용 |
|---|---|---|
| 3-1 | **`SITUATION_CHIPS` + `situation_tags` 를 C(결과) 칸에 연결** — 작문 1칸 즉시 제거 | **자산이 이미 다 있다. 연결만** |
| 3-2 | 한 줄 입력 → **규칙 파서**로 A/B 구조화 → "맞나요?" 1탭 | 주제 라우팅 0.477(기준선 3.8배)로 성립 |
| 3-3 | 사건 직후 1탭 → 저녁에 A·C 채우기(리마인드) | `MSG-001` 인프라 재사용 |

### Layer 4 — 과금·LLM 비용

| # | 할 일 | 근거 |
|---|---|---|
| 4-1 | **안전을 탐지에 걸지 않는다** — 상시 문구(1-2)가 보장, LLM 은 **강조 승격기** | KE-T5: LLM 으로도 vet 100% 불가, 100% 요구 시 과잉 의뢰 **35%** |
| 4-2 | 무료 확대의 통화 = **LLM 호출이 아니라 콘텐츠 접근권** | 2-1 번들 적재가 이걸 가능하게 한다 |
| 4-3 | `compute_dedupe_key` **배선** (현재 호출처 0) · `AI_TTL_7D_HOURS` 캐시 배선 | 인프라가 만들어져 놀고 있다 |
| 4-4 | 프롬프트 **등급 분리** — 무료 축약본 / PRO 정밀본 (현행 단일 ~8,100 토큰) | 비용은 호출 수가 아니라 **토큰 수의 함수** |
| 4-5 | 무료 비중 **수치 확정** | **대기** — 코퍼스 재라벨 적용 후 |

## 4. 착수 순서 (권장)

```
1-1 dog_id UNIQUE  ─┐
1-2 상시 안전 문구  ─┼─ 병렬 가능 (서로 독립)
3-1 칩 연결        ─┘
        ↓
2-1 번들 적재 → 2-3 빌드타임 필터 → 2-2 증상 진입
        ↓
2-5 막힘 처방 시트  ← 이식 최대 이점이 화면에 나오는 지점
        ↓
2-6 Day 축 재설계 → 2-7 A/B/C 제거
        ↓
4-3 dedupe 배선 → 4-4 프롬프트 분리 → 4-5 무료 비중 확정
```

**1-2 · 3-1 이 가장 싸고 체감이 크다.** 둘 다 자산이 이미 있고 연결만 하면 된다.

## 5. 사람 판정 대기 (5건)

1. **야근 브랜치 7커밋을 `main` 에 병합**할까
2. **코퍼스 재라벨 10건을 tailtree 에 적용**할까 (특히 [22] P0 — 심장병이 `none` 으로 남으면 이 코퍼스로 재는 모든 실험이 오염된다)
3. **Backend venv 생성** 승인 (1-1 의 게이트 해제)
4. **2-4 108스텝 처리** — 폐기 / 대안 레이어 / 병합 중 택1
5. **4-5 무료 비중 수치** — 2번 이후

## 6. SSOT 링크

| 문서 | 내용 |
|---|---|
| `docs/ref/rnd/TRAINING-GRAPH-PORT-CONVERGE-LOOP-2026-08-18.md` | 수렴 1·2·3차 (0.368 → 0.776 → 0.831 → 0.859) |
| `docs/ref/rnd/PRODUCT-RESHAPE-CONVERGE-LOOP-2026-08-18.md` | 수렴 4차 (0.563 → 0.851) + KE-T4/T5 |
| `docs/goals/GOAL-ke-t1~t5-*.md` | 킬-실험 5종 계약·실행기록 |
| `docs/status/AUDIT-TRAINING-GRAPH-PORT-2026-08-18.md` | 감사 1 — 설계 워크스루 P0 3 |
| `docs/status/AUDIT-CORPUS-RELABEL-2026-08-18.md` | 감사 2 — 정답지 재라벨 |
| `docs/status/야근-2026-08-18.md` | 야근 보고 |
| `docs/status/MISSING-AND-UNIMPLEMENTED.md §0-A` | 백로그 등록분 |
