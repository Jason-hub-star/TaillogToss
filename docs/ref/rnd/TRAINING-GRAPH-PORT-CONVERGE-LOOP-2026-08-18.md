# 수렴 루프 — tailtree 훈련 그래프를 TaillogToss로 이식 (2026-08-18)

착수 전 게이트. 코드 0줄. 산출 = 수렴 플랜 + 불가역 크럭스 1 + 가장 싼 킬-실험 1.
선행 판정: `조준`(2026-08-18) — 3목표 중 ②UIUX·③가격은 상충, ①훈련추천만 착수 가능.

## 0. 봉인 루브릭 (라운드 1 전 고정 — 이후 불변)

| 차원 | 뜻 | 가중치 |
|---|---|---|
| **F** 실현성 | 토스 미니앱 심사·RN·현 DB 스키마 제약 안에서 실제로 도는가 | 0.20 |
| **E** 근거성 | 주장마다 실측·`file:line`·폐기이력. 날조 0 | 0.15 |
| **G** 결함커버리지 | 마이그레이션·회귀·수익 게이팅·안전 리스크를 덮는가 | 0.20 |
| **N** 신규성·해자 | 현행(룰 A/B/C + 고정 커리큘럼 7) 대비 유저 가치 증분 | 0.15 |
| **C** 비용·시간 현실성 | 1인 개발 기준 착수~출시 | 0.15 |
| **K** 반증가능성 | 계획이 아니라 실험으로 닫히는 크럭스가 지목됐나 | 0.15 |

## 1. 기준선 실측 (라운드 전 확보)

| 항목 | TaillogToss (현행) | tailtree (소스) |
|---|---|---|
| 훈련 데이터 | 커리큘럼 **7** · 일차 35 · 스텝 **109** (`src/lib/data/catalog.json`) | 노드 **1,412** · 엣지 **1,604** (`data/taxonomy/manifest.json` v1.4.0) |
| 실행 콘텐츠 | 스텝 109 | **how 카드 214** — 커버리지 **15.2%** (214/1412) |
| 진입 축 | `BehaviorType` **10종** (`src/types/dog.ts:163`) | 증상 **17종** 폐쇄집합 (`symptoms.json`) |
| 추천 | 룰 A/B/C variant (`recommendation/engine.ts:71`) | 증상→`symptom-index`(214)→노드→선행 사다리 |
| 진행률 | `user_training_status(user_id, dog_id, curriculum_id, stage_id, step_number)` | (없음 — 앱 미구현) |
| 데이터 총량 | — | **2.06 MB** (nodes 1.19 + how 0.35 + edges 0.24 + view 0.31 + 기타) |

how 카드 구조: `oneline` + `steps[5]` + `stuck[{when, then, node}]` + `{{name}}` 개인화.
노드 구조: `id`(`skill.mechanics.marker`) · `label` · `type` · `col`(깊이) · `dom` · `age` · `mastery` · `cgc` · `assessmentPrompt` · `evidence[]`.

증상 17 중 **7종이 `BehaviorType` 10에 없다** — `marking` · `potty` · `handling` · `travel` · `cues` · `energy` · `tricks`. 이게 이식의 실제 가치 증분이다.

## 2. 점수 궤적

| R | 플랜 | F | E | G | N | C | K | **평균** | 새 생존결함 |
|---|---|---|---|---|---|---|---|---|---|
| 0 | P0 통째 이식 | .35 | .50 | .25 | .70 | .30 | .15 | **0.368** | 6 (1 기각) |
| 1 | P1 그래프 서버화 | .55 | .70 | .45 | .60 | .50 | .20 | **0.500** | 3 |
| 2 | P2 좌표계 재사용 | .70 | .80 | .65 | .60 | .60 | .25 | **0.608** | 3 |
| 3 | P3 게이트·분리·크럭스 격리 | .75 | .85 | .75 | .65 | .65 | .80 | **0.743** | 1 |
| 4 | P4 임계 근거화 | .78 | .88 | .78 | .65 | .68 | .88 | **0.776** | **0** |
| 5 | (반박만) | .78 | .88 | .78 | .65 | .68 | .88 | **0.776** | **0** |

**정지**: 무수확 2연속 + 개선률 R3→R4 +4.4%(<5%) 2라운드 → **수렴 선언**.
R1에서 N이 .70→.60으로 **내려간 것**은 게이밍 방지 기록이다 — 통째 대체를 포기해 야심이 실제로 줄었다.

## 3. 라운드별 결함과 적대 판정

### R1 — P0(통째 이식) 해체

| # | 결함 | 판정 | 근거 |
|---|---|---|---|
| D1 | **좌표계 충돌** — 진행률은 `(curriculum_id, stage_id, step_number)`, 그래프는 `node.id`. 기존 진행 데이터 이행 경로 없음 | CONFIRMED | `toss_project_init.sql:413-425` |
| D2 | 번들 100MB 심사 제한 압박 | **REFUTED** | 실측 **2.06MB** = 제한의 2%. 기각 |
| D3 | **A/B/C variants 소실** — 현 스텝마다 `instruction_override`로 개체 적응. tailtree엔 없음 → `recommendPlan` 산출물이 갈 곳을 잃음 | CONFIRMED | `published/…/curriculum.ts` `step()` |
| D4 | **PRO 게이팅 축 소실** — `access:'free'\|'pro'`는 커리큘럼 단위. 노드엔 그 축이 없음 | CONFIRMED | 같은 파일 |
| D5 | **how 커버리지 15.2%** — 1,412 중 1,198 노드는 탭해도 실행 콘텐츠 없음 | CONFIRMED | 실측 |
| D6 | **증상17 ↔ BehaviorType10 회귀** — 설문·코칭·분석·알림이 전부 10종에 결박 | CONFIRMED | `types/dog.ts:163` |

→ **P1**: 통째 이식 폐기. 그래프는 서버(Supabase), 앱은 경로만 수신. 커리큘럼 7 **유지**, 증상 17은 병행 진입점으로 **추가**(교체 아님) → D6 해소.

### R2 — 스키마가 정한 한계

| # | 결함 | 판정 | 근거 |
|---|---|---|---|
| D7 | `curriculum_id`/`stage_id`가 **`String(50)`** — 합성 경로 id(`path:{uuid}:{node}` ≈ 72자)는 삽입 실패 | CONFIRMED | `Backend/app/shared/models.py:522-523` |
| D8 | **UNIQUE에 `dog_id`가 없다** — `UNIQUE(user_id, curriculum_id, stage_id, step_number)`. 다견 사용자가 같은 스텝에 오면 충돌. `/dog/switcher`는 이미 `Done` | CONFIRMED | `toss_project_init.sql:425` + 모델엔 `UniqueConstraint` 선언 자체가 없음(모델↔DB 드리프트) |
| D9 | 경로 생성이 API면 오프라인·지연 시 아카데미 빈 화면 | PLAUSIBLE | — |

**적대 반박** — *"2.06MB면 그냥 번들에 넣어라"*: **부분 채택**. 용량은 문제없으나 번들에 넣으면 **콘텐츠 갱신마다 앱 재심사**가 걸린다(현 `published/` 버전 교체 = 앱 배포와 같은 함정의 연장). → 하이브리드 채택: **경로 계산은 서버, how 카드는 클라이언트 캐시**.

→ **P2**: 새 좌표계를 만들지 않는다. **goal node id를 그대로 `curriculum_id`**(`skill.self_regulation.wait` = 26자, 50자 이내), 사다리 depth를 `stage_id`, how step index를 `step_number`로 매핑 → D1·D7 동시 해소, 진행률 테이블 **무변경**.
D8은 **이식과 무관한 기존 버그**로 분리 — 선결 마이그레이션.

### R3 — 크럭스 격리

| # | 결함 | 판정 | 근거 |
|---|---|---|---|
| D10 | PRO 게이팅 재설계 미정(D4 미해결) → 수익 모델 붕괴 | 생존 | 적대 반박 *"how 카드 수로 팔면 된다"* → **기각**: 기존 구독자는 커리큘럼 단위 access로 구매했다. 소비 단위 변경 = 구매 가치 변경 = 환불·불만. 계획으로 못 닫음 |
| D11 | **미검수 콘텐츠 노출** — tailtree wave8은 커밋 기준 "검수 대기". 훈련 조언은 안전 민감(물림·공격성) | CONFIRMED | tailtree `d12db17` "검수 대기" |
| D12 | how 15.2%가 **경로 위에서** 어떻게 나오는지 미측정. 전체 평균일 뿐이고, 사다리에 연속 빈칸이 나오면 그 경로는 못 쓴다 | 생존 | 적대 반박 *"214장이면 충분"* → **기각**(근거 없음) |

→ **P3**:
- **노출 게이트**: `provenance.review` 통과 노드만 앱에 노출 (필드가 이미 있다) → D11
- **A/B/C를 how 필터로 재해석** — C(노령·통증)면 저강도·짧은 스텝 우선. variants를 버리지 않고 축을 옮긴다 → D3
- **PRO 게이팅은 이식 범위에서 제외**하고 ③가격 과제로 이관 — 이식이 수익모델 변경을 인질로 잡지 않게 → D10 격리
- D12를 **불가역 크럭스**로 승격 + 킬-실험 정의

### R4 — 임계의 근거화

| # | 결함 | 판정 |
|---|---|---|
| D13 | KE 통과 기준 "70%"에 근거 없음 = 임의 임계 | 채택·수정 |

**수정**: 절대 임계를 버리고 **비교 기준**으로. 현행 커리큘럼의 등가 지표(스텝 109가 `BehaviorType` 10을 덮는 경로 커버리지)를 **같은 스크립트로** 재고, 신 경로가 **현행 이상**일 때만 통과. 현행이 기준선이면 "더 나빠지지 않음"이 보장된다.

### R5 — 무수확 2 → 수렴

새 생존결함 0. 제기된 것은 전부 D1~D13의 재진술.

## 4. 수렴 플랜 (P4)

**한 줄**: 커리큘럼 7을 **없애지 않고**, 그래프에서 생성되는 **개인 경로의 뷰**로 만든다. 좌표계·스키마·수익모델은 건드리지 않는다.

| # | 내용 | 근거 결함 |
|---|---|---|
| 1 | **선결**: `user_training_status` UNIQUE에 `dog_id` 추가 + SQLAlchemy `UniqueConstraint` 선언(모델↔DB 드리프트 해소) | D8 |
| 2 | 그래프·how를 Supabase 테이블로 적재. 앱 번들에 넣지 않음(재심사 회피) | D2 반박 |
| 3 | 진입: 증상 17 병행 추가. `BehaviorType` 10은 **유지**(설문·코칭·알림 무회귀), 17→10 매핑 테이블로 공존 | D6 |
| 4 | 경로 → 진행률 매핑: `curriculum_id` := goal node id / `stage_id` := 사다리 depth / `step_number` := how step index. **스키마 무변경** | D1·D7 |
| 5 | A/B/C variant → how 카드 **선택 필터**로 재해석 | D3 |
| 6 | `provenance.review` 통과 노드만 노출. 미검수는 그래프에 있어도 앱에 안 뜬다 | D11 |
| 7 | PRO 게이팅은 **범위 밖** — ③가격 과제로 이관 | D10 |

## 5. 불가역 크럭스 (계획으로 못 닫는 단 하나)

> **how 214장이 실제 증상→목표 경로를 "연속으로" 채우는가.**

15.2%는 전체 평균이다. 사다리 위에서 how 없는 노드가 **연속 2개 이상** 나오면 유저는 그 지점에서 막히고, 그 경로는 커리큘럼을 대체하지 못한다. 문서로도 회의로도 못 닫는다 — 세어봐야 한다.

## 6. 가장 싼 킬-실험 (KE-T1)

**앱 코드 0줄 · 스크립트 1개 · 반나절.**

1. `BehaviorType` 10종 × severity 조합(또는 기존 설문 실데이터 N건)으로 목표 노드를 뽑는다
2. `edges.json`으로 선행 사다리를 역추적해 경로를 만든다
3. 경로 위 노드 중 **how 보유 비율**과 **최대 연속 빈칸 길이**를 센다
4. **같은 스크립트로 현행 커리큘럼**(스텝 109)의 등가 지표를 잰다

| 판정 | 조건 |
|---|---|
| **통과** | 상위 10개 증상 경로에서 how 커버리지가 **현행 이상** AND 최대 연속 빈칸 **≤ 1** |
| **미통과** | 이식 착수 금지. 먼저 tailtree에서 `/웨이브`로 빈칸 노드의 how를 채운다 |

미통과 시 할 일이 "이식"이 아니라 "**콘텐츠 생산**"으로 바뀐다 — 그게 이 실험의 값어치다.

## 7. 사후 정정 — KE-T1 결과 (2026-08-18)

**D5·크럭스의 전제가 오측정이었다.** how 커버리지 15.2%는 분모를 노드 1,412로 잡은 값인데, 그중 **1,102개가 `.sN` 스텝 노드**로 how가 설계상 붙지 않는다. 올바른 분모는 **원자 310**, 커버리지는 **207/310 = 66.8%**.

KE-T1 실행 결과 **PASS**: 경로 고유성 0.90(기준선 0.70) · 최대 연속 빈칸 **0** · 경로당 how 최소 16. 행동문제 도메인 10종은 결손 0이고, how 없는 원자 103개는 전부 스포츠·취미 도메인(trick·nosework·rally·agility·disc·fitness·dance)이다.

⇒ **크럭스 종료, 이식 착수 가능.** P4의 나머지 제약은 유효. 근거: `docs/goals/GOAL-ke-t1-path-coverage.md §7`, `docs/ref/rnd/KE-T1-RESULT.json`.

## 8. 다음

~~KE-T1을 `골`로 계약화해 1패스 실행.~~ → **완료·PASS**(§7). 다음은 수렴 플랜 P4의 **선결 1번**(`user_training_status` UNIQUE에 `dog_id` 추가)부터.
